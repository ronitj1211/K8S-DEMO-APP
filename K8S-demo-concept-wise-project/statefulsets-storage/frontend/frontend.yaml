apiVersion: apps/v1
kind: Deployment
metadata:
  name: counter-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: counter-ui
  template:
    metadata:
      labels:
        app: counter-ui
    spec:
      containers:
        - name: ui
          image: stateful-counter-ui:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: counter-ui
spec:
  type: NodePort
  selector:
    app: counter-ui
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30094
