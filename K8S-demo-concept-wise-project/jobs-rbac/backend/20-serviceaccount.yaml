# A ServiceAccount is the IDENTITY a Pod uses when calling the K8s API.
# Every Pod runs as some ServiceAccount (the namespace's "default" if you
# don't specify). We give the job below a dedicated SA so we can grant it
# only the permissions it needs.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pod-reader
  namespace: team-alpha
