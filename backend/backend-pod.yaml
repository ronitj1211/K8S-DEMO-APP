apiVersion: v1
kind: Pod
metadata:
  name: backend-pod
  labels:
    app: backend
spec:
  containers:
    - name: backend
      image: k8s-demo-backend:v1
      imagePullPolicy: Never
      ports:
        - containerPort: 3001
